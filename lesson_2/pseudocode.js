// a function that returns the sum of two numbers

// START 

// function(number1, number 2)
// SET sum = number1 + number2
// return sum

//END


// a function that takes an array of strings, and returns a string that is all those strings concatenated together

//START
// function(arrayOfStrings)
// SET concatenatedString = ""
// SET iterator = 0
// WHILE (iterator < lengthOfArray)
// concatenatedString += value within arrayOfStrings at position "iterator"
// SET iterator = iterator + 1
//
//Print concatenatedString



// a function that takes an array of integers, and returns a new array with every other element
//START
//function(arrayOfIntegers)
// SET newArray = [];
// SET iterator = 0
// While (iterator < length of newArray)
// Check if iterator is even or zero. 
// if yes:
//    Push value of arrayOfIntegers @ position Iterator to newArray
// return newArray;
//END
